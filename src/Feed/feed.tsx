import React from 'react'
import CardItem from '../CardItem/cardItem'
import { User, mockedPost, mockedSkills, mockedUsers, Post, Skill } from '../Model/User'

const Feed: React.FunctionComponent<{}> = (props) => {

    return (
        <CardItem/>
    )
}

export default Feed